# 测试用例总览

## 项目测试结构

### 后端测试 (`src/test/java/`)
```
src/test/java/
├── com/campus/
│   ├── activity/
│   │   ├── controller/          # Controller层API测试
│   │   │   ├── ActivityControllerTest.java
│   │   │   ├── RegistrationControllerTest.java
│   │   │   ├── CommentControllerTest.java
│   │   │   ├── SearchControllerTest.java
│   │   │   └── ActivityCollectControllerTest.java
│   │   └── service/            # Service层单元测试
│   │       ├── ActivityServiceTest.java
│   │       ├── RegistrationServiceTest.java
│   │       ├── CommentServiceTest.java
│   │       ├── SearchServiceTest.java
│   │       └── ActivityCollectServiceTest.java
│   └── user/
│       ├── controller/          # Controller层API测试
│       │   └── UserControllerTest.java
│       └── service/             # Service层单元测试
│           ├── UserServiceLoginTest.java
│           ├── UserServiceRegisterTest.java
│           └── ForgotPasswordTest.java
```

### 前端测试 (`web/test/`)
```
web/test/
├── unit/                       # 单元测试（使用Mock）
│   ├── activity.test.ts
│   ├── user.test.ts
│   ├── registration.test.ts
│   ├── collect.test.ts
│   ├── comment.test.ts
│   ├── search.test.ts
│   └── request.test.ts
├── api/                        # API集成测试（真实HTTP请求）
│   ├── config.ts               # API测试配置
│   ├── user.api.test.ts
│   ├── activity.api.test.ts
│   ├── registration.api.test.ts
│   ├── collect.api.test.ts
│   ├── comment.api.test.ts
│   └── search.api.test.ts
└── component/                   # 组件测试
    ├── test-utils.ts
    ├── Login.test.ts
    └── ActivityCard.test.ts
```

---

## 测试类型说明

### 1. 单元测试 (Unit Test)
**目的**: 测试最小代码单元的功能正确性
**工具**: JUnit 5 + Mockito (后端), Vitest (前端)
**特点**:
- Mock所有外部依赖
- 执行速度快
- 隔离测试单个组件

### 2. API测试 (API Test)
**目的**: 验证前后端接口是否匹配
**工具**: MockMvc (后端), Fetch API (前端)
**特点**:
- 测试真实HTTP请求/响应
- 验证请求路径、参数、header
- 检查响应数据格式

### 3. 组件测试 (Component Test)
**目的**: 验证Vue组件渲染和行为
**工具**: Vue Test Utils + Vitest
**特点**:
- 测试组件渲染
- 测试用户交互
- 测试组件状态变化

---

## 测试运行命令

### 后端测试
```bash
# 运行所有后端测试
cd d:\PBL6-7
.\mvn.cmd test

# 运行指定测试类
.\mvn.cmd test -Dtest=ActivityControllerTest

# 生成Allure报告
.\mvn.cmd test allure:serve
```

### 前端测试
```bash
cd d:\PBL6-7\web

# 运行所有前端测试
npm run test

# 运行单元测试
npm run test:unit

# 运行API集成测试
npm run test:api

# 运行组件测试
npm run test:component

# 生成覆盖率报告
npm run test:coverage
```

---

## 测试覆盖的功能模块

| 模块 | 单元测试 | API测试 | 组件测试 |
|-----|---------|---------|---------|
| 用户管理 | ✅ | ✅ | ✅ |
| 活动管理 | ✅ | ✅ | ✅ |
| 活动报名 | ✅ | ✅ | - |
| 收藏功能 | ✅ | ✅ | - |
| 评论功能 | ✅ | ✅ | - |
| 搜索功能 | ✅ | ✅ | - |

---

## 测试用例优先级

### 高优先级（必须通过）
1. 用户登录/注册
2. 活动CRUD操作
3. 活动报名/取消报名
4. JWT Token认证

### 中优先级（建议通过）
5. 收藏/取消收藏
6. 评论发布/删除
7. 搜索建议/自动补全

### 低优先级（可选）
8. 通知功能
9. 订阅功能
10. 管理功能

---

## 运行测试前的准备

### 后端
1. 确保MySQL数据库已启动
2. 确保后端服务可编译（需要JDK 19）
3. 配置数据库连接

### 前端
1. 安装依赖: `npm install`
2. 确保后端服务运行在 http://localhost:8080
3. API测试需要真实的后端连接
