<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.user.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.campus.user.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="role" property="role"/>
        <result column="contact" property="contact"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
    </resultMap>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM user_info
        WHERE username = #{username}
        AND deleted_at IS NULL
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user_info
        WHERE id = #{id}
        AND deleted_at IS NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_info (username, password, real_name, role, contact, created_at, updated_at)
        VALUES (#{username}, #{password}, #{realName}, #{role}, #{contact}, NOW(), NOW())
    </insert>

    <update id="updateById">
        UPDATE user_info
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="role != null">role = #{role},</if>
            <if test="contact != null">contact = #{contact},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE user_info SET deleted_at = NOW() WHERE id = #{id}
    </update>

</mapper>
