<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.core.mapper.ActivityFavoriteMapper">

    <resultMap id="BaseResultMap" type="com.campus.core.entity.ActivityFavorite">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="activity_id" property="activityId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 插入收藏记录 -->
    <insert id="insert" parameterType="com.campus.core.entity.ActivityFavorite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO activity_favorite (user_id, activity_id, created_at)
        VALUES (#{userId}, #{activityId}, NOW())
    </insert>

    <!-- 删除收藏记录 -->
    <delete id="deleteByUserIdAndActivityId">
        DELETE FROM activity_favorite
        WHERE user_id = #{userId} AND activity_id = #{activityId}
    </delete>

    <!-- 查询用户的收藏记录 -->
    <select id="selectByUserIdAndActivityId" resultMap="BaseResultMap">
        SELECT * FROM activity_favorite
        WHERE user_id = #{userId} AND activity_id = #{activityId}
    </select>

    <!-- 查询用户的所有收藏记录 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM activity_favorite
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 统计活动的收藏数量 -->
    <select id="countByActivityId" resultType="int">
        SELECT COUNT(*) FROM activity_favorite
        WHERE activity_id = #{activityId}
    </select>

</mapper>
